%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% 模板配置示例文件
% VisionXLab Academic Template Configuration
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
% 这个文件展示了所有可用的配置选项
% 将需要的配置复制到 main.tex 的导言区即可
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%% ============================================================
%% 1. 主题颜色配置
%% ============================================================
% 设置全局主题颜色（影响标题、链接等）
% 默认：2E5AA8（蓝色）

% \setthemecolor{2E5AA8}  % 蓝色（默认）
% \setthemecolor{C41E3A}  % 红色
% \setthemecolor{228B22}  % 绿色
% \setthemecolor{8B008B}  % 紫色
% \setthemecolor{FF8C00}  % 橙色


%% ============================================================
%% 2. 标题配置
%% ============================================================

% 2.1 标题字体大小
% 默认：17pt

% \settitlefont{\fontsize{17}{20}\selectfont}  % 默认
% \settitlefont{\fontsize{19}{22}\selectfont}  % 较大
% \settitlefont{\fontsize{15}{18}\selectfont}  % 较小

% 2.2 标题对齐方式
% 默认：居中

% \titlecenter   % 居中（默认）
% \titleleft     % 居左

% 2.3 标题加粗
% 默认：加粗

% \titleboldon   % 加粗（默认）
% \titleboldoff  % 不加粗

% 2.4 标题横线粗度
% 默认：0.5pt

% 同时设置上下横线粗度
% \settitlerulethickness{0.5pt}  % 默认
% \settitlerulethickness{1pt}    % 较粗
% \settitlerulethickness{1.5pt}  % 很粗
% \settitlerulethickness{0.3pt}  % 较细

% 分别设置上下横线粗度
% \settoprulethickness{1pt}      % 上横线粗度
% \setbottomrulethickness{0.5pt} % 下横线粗度


%% ============================================================
%% 3. Logo 配置
%% ============================================================
% Logo 高度（影响页眉中的 logo 大小）
% 默认：10mm

% \setlogoheight{10mm}    % 默认大小
% \setlogoheight{8mm}     % 较小
% \setlogoheight{12mm}    % 较大

% Logo 间距（左侧两个 logo 之间的间距）
% 默认：3mm

% \setlogospacing{3mm}    % 默认间距
% \setlogospacing{5mm}    % 较大间距

% Logo 和标题横线的距离 ⭐ 优雅方案！
% 默认：0mm（logo 停靠在页眉底部）
% 通过调整 logo 在页眉区域内的垂直位置来控制距离
% 优势：不影响正文布局，只在页眉区域内调整

% \setlogotolineshift{0mm}    % 默认位置
% \setlogotolineshift{3mm}    % logo 向上移 3mm，增大距离
% \setlogotolineshift{5mm}    % logo 向上移 5mm，距离更大
% \setlogotolineshift{-3mm}   % logo 向下移 3mm，减小距离


%% ============================================================
%% 4. Abstract 框样式配置
%% ============================================================

% 4.1 开启/关闭 Abstract 边框
% 默认：开启边框

% \abstractboxon          % 显示边框（默认）
% \abstractboxoff         % 不显示边框

% 4.2 Abstract 框颜色
% 默认：使用主题颜色

% \setabstractframecolor{themecolor}  % 使用主题颜色（默认）
% \setabstractframecolor{black}       % 黑色边框
% \setabstractframecolor{gray}        % 灰色边框

% 4.3 Abstract 背景颜色
% 默认：白色

% \setabstractbgcolor{white}         % 白色背景（默认）
% \setabstractbgcolor{gray!10}       % 浅灰色背景
% \definecolor{lightblue}{HTML}{E8F4F8}
% \setabstractbgcolor{lightblue}     % 浅蓝色背景

% 4.4 Abstract 标题对齐方式
% 默认：居中

% \abstracttitlecenter    % 居中（默认）
% \abstracttitleleft      % 居左

% 4.5 Abstract 标题字体
% 默认：\fontsize{10}{12}\selectfont

% \setabstractfont{\fontsize{10}{12}\selectfont}  % 默认
% \setabstractfont{\fontsize{12}{14}\selectfont}  % 较大


%% ============================================================
%% 5. 章节标题配置
%% ============================================================

% 5.1 Section 字体大小
% 默认：12pt

% \setsectionfont{\fontsize{12}{14}\selectfont}   % 默认
% \setsectionfont{\fontsize{14}{16}\selectfont}   % 较大
% \setsectionfont{\fontsize{11}{13}\selectfont}   % 较小

% 5.2 Subsection 字体大小
% 默认：11pt

% \setsubsectionfont{\fontsize{11}{13}\selectfont}  % 默认
% \setsubsectionfont{\fontsize{12}{14}\selectfont}  % 较大


%% ============================================================
%% 6. 章节装饰线配置
%% ============================================================

% 6.1 开启/关闭章节标题下方的装饰线
% 默认：关闭
% 注意：这个设置需要在 \documentclass 之后、\begin{document} 之前

% \sectionlineon          % 显示装饰线
% \sectionlineoff         % 不显示装饰线（默认）

% 6.2 装饰线颜色
% 默认：使用主题颜色

% \setsectionlinecolor{themecolor}  % 使用主题颜色（默认）
% \setsectionlinecolor{red}         % 红色
% \setsectionlinecolor{blue}        % 蓝色

% 6.3 装饰线粗度
% 默认：1.5pt

% \setsectionlinethickness{1.5pt}  % 默认
% \setsectionlinethickness{2pt}    % 较粗
% \setsectionlinethickness{2.5pt}  % 很粗
% \setsectionlinethickness{1pt}    % 较细
% \setsectionlinethickness{0.5pt}  % 很细


%% ============================================================
%% 使用示例
%% ============================================================

% 示例 1：红色主题 + 无 Abstract 边框
% \setthemecolor{C41E3A}
% \abstractboxoff

% 示例 2：绿色主题 + 浅灰背景 + 章节装饰线
% \setthemecolor{228B22}
% \setabstractbgcolor{gray!10}
% \sectionlineon

% 示例 3：紫色主题 + 较大 Logo + 左对齐 Abstract + 调整横线距离
% \setthemecolor{8B008B}
% \setlogoheight{12mm}
% \setlogotolineshift{3mm}
% \abstracttitleleft

% 示例 4：自定义配色方案
% \definecolor{mycolor}{HTML}{FF6B6B}
% \setthemecolor{FF6B6B}
% \setabstractframecolor{mycolor}
% \setsectionlinecolor{mycolor}
% \sectionlineon


%% ============================================================
%% 注意事项
%% ============================================================
%
% 1. 所有配置命令都应该放在 main.tex 的导言区
%    （\documentclass 之后，\begin{document} 之前）
%
% 2. 颜色代码可以是：
%    - HTML 格式：\setthemecolor{2E5AA8}
%    - 预定义颜色：red, blue, green 等
%    - 自定义颜色：先用 \definecolor 定义，再使用
%
% 3. 长度单位可以是：mm, cm, pt, em 等
%
% 4. 修改配置后，需要完全重新编译（删除临时文件）
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
