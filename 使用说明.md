# LaTeX 学术论文模板使用说明

## 📋 目录

1. [项目简介](#项目简介)
2. [项目结构](#项目结构)
3. [快速开始](#快速开始)
4. [详细使用指南](#详细使用指南)
5. [自定义指南](#自定义指南)
6. [常见问题](#常见问题)
7. [注意事项](#注意事项)

---

## 项目简介

**VisionXLab** 专属的的学术论文 LaTeX 模板。模板具有以下特点：

- ✅ **模块化结构**：章节独立文件，便于管理和协作
- ✅ **自定义样式**：基于参考论文风格的专业排版
- ✅ **多 Logo 支持**：首页支持左右共 3 个 logo 展示
- ✅ **丰富示例**：包含图片、表格、算法、公式等各种元素
- ✅ **易于扩展**：清晰的文件组织，方便添加新内容
- ✅ **Overleaf 就绪**：可直接上传到 Overleaf 使用

---

## 项目结构

```
VisionXLab_latex/
├── main.tex                    # 主文件（从这里开始编译）
├── academic_template.cls       # 自定义样式文件
├── references.bib              # 参考文献数据库
├── 使用说明.md                # 本说明文档
│
├── sections/                   # 章节文件夹
│   ├── 1_introduction.tex     # 第 1 章：引言
│   ├── 2_related_work.tex     # 第 2 章：相关工作
│   ├── 3_method.tex           # 第 3 章：方法
│   ├── 4_experiments.tex      # 第 4 章：实验
│   └── 5_conclusion.tex       # 第 5 章：结论
│
├── figures/                    # 图片文件夹
│   ├── logos/                 # Logo 图片（PNG 格式）
│   │   ├── sjtu.png          # 左侧第一个 logo
│   │   ├── lab.png           # 左侧第二个 logo
│   │   └── company.png       # 右侧公司 logo
│   ├── content/               # 正文图片（PDF 格式）
│   │   ├── example_figure.pdf
│   │   ├── architecture.pdf
│   │   └── ...
│   └── README_FIGURES.md      # 图片说明文档
│
└── 参考/                      # 参考材料（可选）
    └── arXiv-2601.03467v2/    # 参考论文源码
```

---

## 快速开始

### 方法一：在 Overleaf 上使用（推荐）

1. **压缩项目文件**
   - 选择以下文件和文件夹：
     - `main.tex`
     - `academic_template.cls`
     - `references.bib`
     - `sections/` 文件夹
     - `figures/` 文件夹
   - 压缩为 `.zip` 文件

2. **上传到 Overleaf**
   - 登录 [Overleaf](https://www.overleaf.com/)
   - 点击 "New Project" → "Upload Project"
   - 上传刚才创建的 `.zip` 文件

3. **编译**
   - Overleaf 会自动识别 `main.tex` 为主文件
   - 点击 "Recompile" 按钮编译
   - 编译器选择：推荐使用 `pdfLaTeX`

### 方法二：本地使用

1. **安装 LaTeX 环境**
   - Windows: [MiKTeX](https://miktex.org/) 或 [TeX Live](https://www.tug.org/texlive/)
   - macOS: [MacTeX](https://www.tug.org/mactex/)
   - Linux: `sudo apt-get install texlive-full`

2. **编译**
   ```bash
   cd VisionXLab_latex
   pdflatex main.tex
   bibtex main
   pdflatex main.tex
   pdflatex main.tex
   ```

   或者使用 `latexmk`（推荐）：
   ```bash
   latexmk -pdf main.tex
   ```

---

## 详细使用指南

### 1. 修改标题和作者信息

编辑 [main.tex](main.tex) 文件，找到以下部分：

```latex
\title{Your Paper Title: A Comprehensive Template for Academic Writing}

\author[1,*]{First Author}
\author[1,2]{Second Author}
\author[2]{Third Author}
\author[1, \dagger]{Fourth Author}

\affiliation[1]{Your University}
\affiliation[2]{Your Institute or Company}

\contribution[*]{Equal contribution}
\contribution[\dagger]{Corresponding Author}
```

**说明：**
- `\title{}`: 论文标题
- `\author[机构编号,标记]{作者姓名}`: 作者姓名和所属机构
- `\affiliation[编号]{机构名称}`: 机构名称
- `\contribution[标记]{说明}`: 作者贡献说明

### 2. 修改摘要

编辑 [main.tex](main.tex) 中的 `\abstract{}` 部分：

```latex
\abstract{
这里填写您的摘要内容。摘要应简洁明了地概括研究动机、方法、
结果和贡献。建议 150-250 词。
}
```

### 3. 添加项目主页和代码链接

在 [main.tex](main.tex) 中修改：

```latex
\checkdata[Project Page]{\url{https://your-project-page.github.io/}}
\checkdata[Code]{\url{https://github.com/your-repo}}
```

### 4. 编写各章节内容

#### Introduction（引言）
编辑 [sections/1_introduction.tex](sections/1_introduction.tex)
- 研究背景和动机
- 现有方法的局限性
- 本文的解决方案
- 主要贡献
- 论文组织结构

#### Related Work（相关工作）
编辑 [sections/2_related_work.tex](sections/2_related_work.tex)
- 按主题组织相关工作
- 使用 `\subsection{}` 划分不同研究方向
- 与本文工作的关系

#### Method（方法）
编辑 [sections/3_method.tex](sections/3_method.tex)
- 方法概述
- 各个组件的详细描述
- 算法伪代码
- 数学公式推导
- 实现细节

#### Experiments（实验）
编辑 [sections/4_experiments.tex](sections/4_experiments.tex)
- 实验设置（数据集、评估指标、基线方法）
- 定量结果（表格）
- 定性结果（图片对比）
- 消融实验
- 超参数分析

#### Conclusion（结论）
编辑 [sections/5_conclusion.tex](sections/5_conclusion.tex)
- 工作总结
- 局限性
- 未来工作方向

### 5. 添加图片

#### Logo 图片（首页）
将您的 logo 图片（PNG 格式）放入 `figures/logos/` 文件夹：
- `sjtu.png`: 左侧第一个 logo
- `lab.png`: 左侧第二个 logo
- `company.png`: 右侧公司 logo

#### 正文图片（推荐使用 PDF 矢量图）
将图片放入 `figures/content/` 文件夹，然后在 LaTeX 中引用：

```latex
% 单栏图片
\begin{figure}[t]
\centering
\includegraphics[width=0.9\linewidth]{figures/content/your_figure.pdf}
\caption{图片说明}
\label{fig:your_label}
\end{figure}

% 双栏宽度图片
\begin{figure*}[ht]
\centering
\includegraphics[width=\linewidth]{figures/content/your_figure.pdf}
\caption{图片说明}
\label{fig:your_label}
\end{figure*}

% 子图
\begin{figure}[t]
\centering
\begin{subfigure}{0.48\linewidth}
    \includegraphics[width=\linewidth]{figures/content/sub_a.pdf}
    \caption{子图 A}
\end{subfigure}
\hfill
\begin{subfigure}{0.48\linewidth}
    \includegraphics[width=\linewidth]{figures/content/sub_b.pdf}
    \caption{子图 B}
\end{subfigure}
\caption{整体说明}
\label{fig:subfigures}
\end{figure}
```

### 6. 添加表格

参考 [sections/2_related_work.tex](sections/2_related_work.tex) 和 [sections/4_experiments.tex](sections/4_experiments.tex) 中的示例：

```latex
\begin{table}[t]
\centering
\caption{表格标题}
\label{tab:your_label}
\tablestyle{4pt}{1.2}  % 设置列间距和行高
\begin{tabular}{lccc}
\toprule
\textbf{列1} & \textbf{列2} & \textbf{列3} & \textbf{列4} \\
\midrule
数据1 & 数据2 & 数据3 & 数据4 \\
数据5 & 数据6 & 数据7 & 数据8 \\
\bottomrule
\end{tabular}
\end{table}
```

### 7. 添加算法

参考 [sections/3_method.tex](sections/3_method.tex) 中的示例：

```latex
\begin{algorithm}[t]
\caption{算法名称}
\label{alg:your_label}
\begin{algorithmic}[1]
\Require 输入参数
\Ensure 输出结果
\State 初始化
\For{条件}
    \State 执行操作
\EndFor
\State \Return 结果
\end{algorithmic}
\end{algorithm}
```

### 8. 添加公式

```latex
% 行内公式
这是行内公式 $y = f(x)$。

% 独立公式
\begin{equation}
\label{eq:your_label}
\mathcal{L} = \sum_{i=1}^{N} \ell(y_i, \hat{y}_i)
\end{equation}

% 引用公式
如方程~\ref{eq:your_label} 所示...
```

### 9. 管理参考文献

编辑 [references.bib](references.bib) 文件，添加文献条目：

```bibtex
@inproceedings{author2024method,
  title={Paper Title},
  author={Author, First and Author, Second},
  booktitle={Conference Name},
  year={2024},
  pages={100--110}
}
```

在正文中引用：
```latex
如文献~\cite{author2024method} 所示...
```

---

## 自定义指南

本模板提供了丰富的配置选项，所有配置都可以在 [main.tex](main.tex) 的导言区（`\documentclass` 之后，`\begin{document}` 之前）进行修改。

📖 **完整配置示例**：请查看 [template_config.tex](template_config.tex) 文件，其中包含所有可用配置选项的详细说明。

### 1. 主题颜色配置 🎨

在 [main.tex](main.tex) 导言区添加：

```latex
% 修改全局主题颜色（影响标题、链接、边框等）
\setthemecolor{2E5AA8}  % 蓝色（默认）
```

**常用颜色代码**：
- `2E5AA8` - 蓝色（默认）
- `C41E3A` - 红色
- `228B22` - 绿色
- `8B008B` - 紫色
- `FF8C00` - 橙色
- `000000` - 黑色

**自定义颜色**：
```latex
\definecolor{mycolor}{HTML}{FF6B6B}
\setthemecolor{FF6B6B}
```

---

### 2. Logo 配置 🖼️

#### 2.1 修改 Logo 文件

将您的 logo 图片放入 `figures/logos/` 文件夹，替换以下文件：
- `sjtu.png` - 左侧第一个 logo
- `lab.png` - 左侧第二个 logo
- `company.png` - 右侧公司 logo

**Logo 要求**：
- 格式：PNG 格式
- 分辨率：建议 300 DPI 或更高
- 背景：透明背景
- 尺寸：高度建议 300-500px

#### 2.2 调整 Logo 大小

在 [main.tex](main.tex) 导言区添加：

```latex
% 设置 logo 高度（默认 10mm）
\setlogoheight{10mm}   % 默认大小
\setlogoheight{8mm}    % 较小
\setlogoheight{12mm}   % 较大
```

#### 2.3 调整 Logo 间距

```latex
% 设置左侧两个 logo 之间的间距（默认 3mm）
\setlogospacing{3mm}   % 默认
\setlogospacing{5mm}   % 较大间距
```

#### 2.4 调整 Logo 和标题横线的距离 ⭐

**推荐方法：使用垂直偏移调整（不影响正文布局）**

在 [main.tex](main.tex) 导言区添加：

```latex
% 调整 logo 在页眉区域的垂直位置（默认 0mm）
\setlogotolineshift{0mm}    % 默认位置
\setlogotolineshift{3mm}    % logo 向上移 3mm，增大与横线的距离
\setlogotolineshift{5mm}    % logo 向上移 5mm，距离更大
\setlogotolineshift{-3mm}   % logo 向下移 3mm，减小与横线的距离
```

**工作原理：**
- 页眉区域高度固定，**不影响正文位置**
- 只调整 logo 在页眉区域内的垂直位置
- 正值：logo 向上移动，增大与横线的距离
- 负值：logo 向下移动，减小与横线的距离

**参数建议：**
- `5mm` - 距离很大
- `3mm` - 距离较大
- `0mm` - 默认距离
- `-3mm` - 距离较小
- `-5mm` - 距离很小

#### 2.5 添加/删除 Logo

如果只需要 1-2 个 logo，编辑 [academic_template.cls](academic_template.cls:222)：

```latex
% 只显示左侧一个 logo
\fancypagestyle{firststyle}{
    \fancyhead[L]{%
        \raisebox{-\height}[0pt][0pt]{%
            \includegraphics[height=\logoheight]{figures/logos/sjtu.png}%
        }%
    }
    \fancyhead[R]{}  % 右侧留空
}
```

---

### 3. 标题配置 📰

#### 3.1 修改标题字体大小

在 [main.tex](main.tex) 导言区添加：

```latex
% 设置标题字体大小（默认 17pt）
\settitlefont{\fontsize{17}{20}\selectfont}  % 默认
\settitlefont{\fontsize{19}{22}\selectfont}  % 较大
\settitlefont{\fontsize{15}{18}\selectfont}  % 较小
```

#### 3.2 标题对齐方式

```latex
\titlecenter   % 居中（默认）
\titleleft     % 居左
```

#### 3.3 标题加粗

```latex
\titleboldon   % 加粗（默认）
\titleboldoff  % 不加粗
```

#### 3.4 标题横线粗度

**同时设置上下横线：**

```latex
% 设置标题上下横线的粗度（默认 0.5pt）
\settitlerulethickness{0.5pt}  % 默认
\settitlerulethickness{1pt}    % 较粗
\settitlerulethickness{1.5pt}  % 很粗
\settitlerulethickness{0.3pt}  % 较细
```

**分别设置上下横线：**

```latex
% 上横线较粗，下横线较细
\settoprulethickness{1.5pt}    % 上横线
\setbottomrulethickness{0.5pt} % 下横线

% 或者只设置其中一条
\settoprulethickness{2pt}      % 只修改上横线
```

**示例组合**：
```latex
% 大标题 + 居左对齐 + 不加粗 + 粗横线
\settitlefont{\fontsize{19}{22}\selectfont}
\titleleft
\titleboldoff
\settitlerulethickness{1pt}
```

---

### 4. Abstract 框样式配置 📝

#### 4.1 开启/关闭边框

在 [main.tex](main.tex) 导言区添加：

```latex
\abstractboxon     % 显示边框（默认）
\abstractboxoff    % 不显示边框
```

#### 4.2 修改边框颜色

```latex
\setabstractframecolor{themecolor}  % 使用主题颜色（默认）
\setabstractframecolor{black}       % 黑色边框
\setabstractframecolor{gray}        % 灰色边框
```

#### 4.3 修改背景颜色

```latex
\setabstractbgcolor{white}          % 白色背景（默认）
\setabstractbgcolor{gray!10}        % 浅灰色背景

% 自定义背景色
\definecolor{lightblue}{HTML}{E8F4F8}
\setabstractbgcolor{lightblue}
```

#### 4.4 标题对齐方式

```latex
\abstracttitlecenter   % 居中（默认）
\abstracttitleleft     % 居左
```

#### 4.5 修改标题字体大小

```latex
\setabstractfont{\fontsize{10}{12}\selectfont}  % 默认
\setabstractfont{\fontsize{12}{14}\selectfont}  % 较大
```

**示例组合**：
```latex
% 无边框 + 浅灰背景
\abstractboxoff
\setabstractbgcolor{gray!10}
```

---

### 5. 章节标题样式配置 📑

#### 5.1 修改字体大小

```latex
% Section 字体（默认 12pt）
\setsectionfont{\fontsize{12}{14}\selectfont}   % 默认
\setsectionfont{\fontsize{14}{16}\selectfont}   % 较大

% Subsection 字体（默认 11pt）
\setsubsectionfont{\fontsize{11}{13}\selectfont}  % 默认
\setsubsectionfont{\fontsize{12}{14}\selectfont}  % 较大
```

#### 5.2 添加章节装饰线

在 [main.tex](main.tex) 导言区添加：

```latex
\sectionlineon     % 在章节标题下方显示装饰线
\sectionlineoff    % 不显示装饰线（默认）
```

**⚠️ 重要提示**：
- 修改后需要**删除 `tmp/` 文件夹**中的所有临时文件
- 然后**完全重新编译**（多次编译确保生效）
- 在 VSCode 中：删除 tmp 文件夹后保存 main.tex 即可

**测试文件**：
- 使用 [test_sectionline.tex](test_sectionline.tex) 快速测试效果

#### 5.3 修改装饰线颜色

```latex
\setsectionlinecolor{themecolor}  % 使用主题颜色（默认）
\setsectionlinecolor{red}         % 红色
\setsectionlinecolor{blue}        % 蓝色
```

#### 5.4 修改装饰线粗度

```latex
% 设置章节装饰线的粗度（默认 1.5pt）
\setsectionlinethickness{1.5pt}  % 默认
\setsectionlinethickness{2pt}    % 较粗
\setsectionlinethickness{2.5pt}  % 很粗
\setsectionlinethickness{1pt}    % 较细
\setsectionlinethickness{0.5pt}  % 很细
```

**完整示例**：

```latex
\documentclass[]{academic_template}

% 开启装饰线并设置颜色和粗度
\sectionlineon
\setsectionlinecolor{red}
\setsectionlinethickness{2pt}

\begin{document}
...
```

**效果预览**：
- 关闭装饰线：`## Section Title`
- 开启装饰线：`## Section Title`
  `________________`（彩色线，粗度可调）

---

### 6. 启用双栏模式 📰

编辑 [main.tex](main.tex:1)，将第一行改为：

```latex
\documentclass[twocolumn]{academic_template}
```

---

### 7. 添加目录 📚

在 [main.tex](main.tex) 中取消注释：

```latex
\newpage
\tableofcontents
\newpage
```

---

### 8. 添加附录 📎

在 [main.tex](main.tex) 中取消注释：

```latex
\clearpage
\beginappendix
\input{sections/appendix}
```

然后创建 `sections/appendix.tex` 文件。

---

## 配置示例集合 🎨

### 示例 1：经典学术风格

```latex
% 蓝色主题 + 有边框 Abstract
\setthemecolor{2E5AA8}
\abstractboxon
```

### 示例 2：现代简约风格

```latex
% 黑色主题 + 无边框 Abstract + 章节装饰线
\setthemecolor{000000}
\abstractboxoff
\sectionlineon
```

### 示例 3：温暖红色主题

```latex
% 红色主题 + 浅灰背景 + 较大 Logo + 增大横线距离
\setthemecolor{C41E3A}
\setabstractbgcolor{gray!10}
\setlogoheight{12mm}
\setlogotolineshift{3mm}
```

### 示例 4：清新绿色主题

```latex
% 绿色主题 + 彩色装饰线
\setthemecolor{228B22}
\sectionlineon
\setsectionlinecolor{green}
```

### 示例 5：完全自定义

```latex
% 自定义颜色方案
\definecolor{myblue}{HTML}{4A90E2}
\definecolor{mylightblue}{HTML}{E8F4F8}

\setthemecolor{4A90E2}
\setabstractbgcolor{mylightblue}
\setabstractframecolor{myblue}
\abstracttitleleft
\sectionlineon
\setsectionlinecolor{myblue}
\setlogoheight{11mm}
```

---

## 配置位置说明 📍

所有配置命令应放在 [main.tex](main.tex) 的**导言区**：

```latex
\documentclass[]{academic_template}

% ========== 在这里添加配置 ==========
\setthemecolor{C41E3A}
\abstractboxoff
\sectionlineon
% ===================================

\usepackage[toc,page,header]{appendix}
\usepackage{amssymb}
% ... 其他包

\begin{document}
\maketitle
% ... 正文内容
\end{document}
```

---

## 颜色代码参考 🎨

### HTML 颜色格式
使用 6 位十六进制代码：`\setthemecolor{RRGGBB}`

### 常用学术颜色

| 颜色 | 代码 | 效果 |
|------|------|------|
| 牛津蓝 | `002147` | 深蓝色，专业 |
| 剑桥蓝 | `00B0B9` | 青蓝色，活力 |
| 哈佛红 | `A51C30` | 深红色，经典 |
| 斯坦福红 | `8C1515` | 暗红色，稳重 |
| MIT 红 | `A31F34` | 鲜红色，醒目 |
| 普林斯顿橙 | `E87722` | 橙色，温暖 |
| 耶鲁蓝 | `00356B` | 深蓝色，典雅 |

### 在线颜色选择器
- [HTML Color Codes](https://htmlcolorcodes.com/)
- [Adobe Color](https://color.adobe.com/)
- [Coolors](https://coolors.co/)

---

## 高级技巧 💡

### 1. 不同章节使用不同颜色

```latex
% 在章节开头局部修改颜色
{\color{red}\section{特殊章节}}
\section{普通章节}  % 恢复默认颜色
```

### 2. 自定义标题框线粗细

编辑 [academic_template.cls](academic_template.cls:281)，修改：

```latex
\newcommand{\@toptitlebar}{
  \themeblue{\hrule height 0.5pt }  % 修改这里的 0.5pt
  \vskip 6mm
}
```

### 3. 修改字体族

在 [main.tex](main.tex) 导言区添加：

```latex
\usepackage{times}  % 使用 Times 字体
% 或
\usepackage{palatino}  % 使用 Palatino 字体
```

---

## 故障排除 🔧

### Q: 修改颜色后没有效果？
**A**: 删除 `tmp/` 文件夹中的所有文件，然后重新编译。

### Q: Logo 显示不正常？
**A**: 检查：
1. 图片文件是否存在于 `figures/logos/` 文件夹
2. 文件名是否正确（区分大小写）
3. 图片格式是否为 PNG

### Q: 章节装饰线不显示？
**A**: 确保：
1. 使用了 `\sectionlineon` 命令
2. 命令放在 `\begin{document}` 之前
3. **删除 tmp/ 文件夹中的所有临时文件**
4. **完全重新编译**（在 VSCode 中保存文件会自动编译）
5. 尝试编译 [test_sectionline.tex](test_sectionline.tex) 测试文件验证功能

**操作步骤**：
```bash
# 删除临时文件
rm -rf tmp/*

# 或者在 Windows 文件管理器中删除 tmp 文件夹中的所有文件
```

然后在 VSCode 中保存 main.tex，会自动重新编译。

### Q: Abstract 边框颜色无法修改？
**A**: 检查颜色名称是否正确：
- 使用预定义颜色：`red`, `blue`, `green` 等
- 使用自定义颜色：先用 `\definecolor` 定义

---

## 常见问题

### Q1: 编译报错 "File not found"

**A:** 检查以下几点：
1. 确保所有图片文件都存在于指定路径
2. 文件路径使用正斜杠 `/`，不要使用反斜杠 `\`
3. Overleaf 上传时确保文件夹结构完整

### Q2: Logo 不显示或位置不对

**A:**
1. 确认 logo 文件在 `figures/logos/` 文件夹中
2. 检查文件名是否正确（区分大小写）
3. 调整 [academic_template.cls](academic_template.cls) 中的 `\fancypagestyle{firststyle}` 设置

### Q3: 参考文献不显示

**A:**
1. 确保运行了完整的编译流程：
   ```
   pdflatex → bibtex → pdflatex → pdflatex
   ```
2. 检查 `.bib` 文件中的条目格式是否正确
3. 确保在正文中使用了 `\cite{}`

### Q4: 中文显示乱码

**A:** 本模板默认为英文。如需支持中文：
1. 使用 `xelatex` 编译器
2. 在 [main.tex](main.tex) 开头添加：
   ```latex
   \usepackage{ctex}
   ```

### Q5: 如何修改字体大小

**A:** 编辑 [academic_template.cls](academic_template.cls)，找到对应部分修改：
```latex
\newcommand{\sectionfont}{\fontsize{12}{10}\selectfont}
\newcommand{\subsectionfont}{\fontsize{11}{10}\selectfont}
```

---

## 注意事项

### 1. 图片格式
- **Logo**: 使用 PNG 格式，建议 300 DPI，透明背景
- **正文图片**: 强烈推荐使用 PDF 格式的矢量图，保证缩放不失真

### 2. 文件命名
- 避免使用空格和特殊字符
- 推荐使用下划线 `_` 连接单词
- 示例：`example_figure.pdf` ✅  ，`example figure.pdf` ❌

### 3. 编译顺序
完整编译需要运行：
```bash
pdflatex main.tex    # 第一次编译，生成 .aux 文件
bibtex main          # 处理参考文献
pdflatex main.tex    # 第二次编译，插入引用
pdflatex main.tex    # 第三次编译，更新交叉引用
```

### 4. Overleaf 设置
- 编译器：选择 `pdfLaTeX`
- 主文件：确保设置为 `main.tex`
- 自动编译：可以在设置中启用

### 5. 版本控制
- 建议使用 Git 管理 LaTeX 源码
- 定期备份 `.tex`、`.bib`、`.cls` 文件
- Overleaf 提供历史版本功能

### 6. 协作建议
- 每个章节独立文件，方便多人协作
- 使用 Overleaf 的共享功能进行协作
- 定期同步和备份

---

## 技术支持

如有问题，可以参考：
- [Overleaf 文档](https://www.overleaf.com/learn)
- [LaTeX 官方文档](https://www.latex-project.org/help/documentation/)
- [TeX Stack Exchange](https://tex.stackexchange.com/)

---

**祝您论文写作顺利！Good luck with your paper! 🎓**
